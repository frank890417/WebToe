{
  "name": "webtoe-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Web-native dataflow engine for real-time visuals, with TouchDesigner project import",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "bin": {
    "webtoe": "packages/bridge/index.mjs",
    "webtoe-bridge": "packages/bridge/index.mjs"
  },
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "bridge": "node packages/bridge/index.mjs",
    "release:prep": "npm run build && node tools/prep-webtoe-package.mjs",
    "build": "npm run build -w apps/web",
    "preview": "npm run preview -w apps/web",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@webgpu/types": "^0.1.70",
    "playwright-core": "^1.60.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
